name: Coq Proof Verification

on: [push, pull_request]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Run verification in Podman/Docker
      # The ubuntu-latest runner comes with podman pre-installed
      run: bash run_podman.sh
